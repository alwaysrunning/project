define(function (require, exports, module) {
    'use strict';

    var $ = require('jquery');
    //var io = require('common/kit/io/request');
    
    var event = require('lib/core/1.0.0/event/emitter'); 

    var size = module.config().size;

    console.log(size)
});
